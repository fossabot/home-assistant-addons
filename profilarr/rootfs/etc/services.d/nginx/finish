#!/usr/bin/with-contenv bashio
declare APP_EXIT_CODE=${1}

bashio::log.warning "nginx exited with code ${APP_EXIT_CODE}"

if [ "${APP_EXIT_CODE}" -ne 0 ]; then
    bashio::log.error "nginx crashed"
fi
